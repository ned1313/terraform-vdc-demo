# This configuration will create a VPC in AWS and provision it with two public subnets
# and two private subnets.  It will also created a VPN connection object and return as
# output the public IP address of the VPN device

